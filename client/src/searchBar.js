import React from 'react';

const SearchBar = () => (
    <div id="inputSearch">
        <input id="textInputSearch" type="text" placeholder="Enter here.." />
    </div>
);

export default SearchBar;
